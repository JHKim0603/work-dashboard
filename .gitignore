# Generated output — regenerated each run by Update-WorkDashboard.ps1, no need to version it
dashboard.html
email-summary.html
email-subject.txt

# Local API keys, so a run.bat run renders the same cards CI does. Never commit this —
# the repo is public. Keys belong in GitHub Actions secrets for CI, and here for local only.
secrets.local.json

# 진단용으로 내려받는 배포본 스냅샷
live.html
